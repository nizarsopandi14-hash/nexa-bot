<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nexa Dashboard</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Inter', sans-serif; }
        body { background: #0f0f1a; color: white; min-height: 100vh; padding: 40px; }

        .navbar { display: flex; justify-content: space-between; align-items: center; margin-bottom: 60px; }
        .logo { font-weight: 900; font-size: 1.5rem; color: #00d2ff; }

        .dashboard-container {
            max-width: 1000px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 30px;
        }

        .profile-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 30px;
            border-radius: 24px;
            text-align: center;
            backdrop-filter: blur(10px);
        }

        .avatar {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            border: 4px solid #5865F2;
            margin-bottom: 20px;
        }

        .status-badge {
            display: inline-block;
            padding: 5px 15px;
            background: rgba(0, 210, 255, 0.1);
            color: #00d2ff;
            border-radius: 20px;
            font-size: 0.8rem;
            margin-top: 10px;
        }

        .main-content {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 30px;
            border-radius: 24px;
        }

        .command-list { margin-top: 20px; text-align: left; }
        .command-item {
            background: rgba(255, 255, 255, 0.05);
            padding: 15px;
            border-radius: 12px;
            margin-bottom: 10px;
            display: flex;
            justify-content: space-between;
        }

        .btn-logout { color: #ff4757; text-decoration: none; font-weight: bold; font-size: 0.9rem; }
        code { color: #00d2ff; background: rgba(0,0,0,0.3); padding: 2px 6px; border-radius: 4px; }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="logo">NEXA.</div>
        <a href="/logout" class="btn-logout">Sign Out</a>
    </nav>

    <div class="dashboard-container">
        <div class="profile-card">
            <img class="avatar" src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar">
            <h2><%= user.username %></h2>
            <div class="status-badge">Nexa Premium User</div>
        </div>

        <div class="main-content">
            <h3>Music Controller</h3>
            <p style="color: #888; margin-bottom: 20px;">Gunakan perintah di bawah ini pada Server Discord Anda.</p>
            
            <div class="command-list">
                <div class="command-item">
                    <span>Putar Lagu</span>
                    <code>!play [link/judul]</code>
                </div>
                <div class="command-item">
                    <span>Berhentikan</span>
                    <code>!stop</code>
                </div>
                <div class="command-item">
                    <span>Lewati Lagu</span>
                    <code>!skip</code>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
