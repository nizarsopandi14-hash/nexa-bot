<!DOCTYPE html>
<html>
<head>
    <title>Nexa | Login</title>
    <style>
        body { background: #0a0a0f; color: white; font-family: sans-serif; display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0; }
        .login-card { 
            background: rgba(255,255,255,0.05); 
            width: 400px; padding: 40px; border-radius: 20px; text-align: center;
            backdrop-filter: blur(10px); border: 1px solid rgba(255,255,255,0.1);
        }
        .banner { width: 100%; height: 120px; background: url('/banner.jpg') center/cover; border-radius: 10px; margin-bottom: 20px; }
        .logo { width: 80px; margin-top: -50px; border: 5px solid #0a0a0f; border-radius: 50%; }
        input { width: 100%; padding: 10px; margin: 10px 0; border-radius: 5px; border: none; }
        .btn { display: block; width: 100%; padding: 12px; border-radius: 5px; text-decoration: none; font-weight: bold; margin-top: 10px; cursor: pointer; border: none; }
        .btn-discord { background: #5865F2; color: white; }
        .btn-email { background: #333; color: white; }
    </style>
</head>
<body>
    <div class="login-card">
        <div class="banner"></div>
        <img src="/logo.png" class="logo" alt="Nexa Logo">
        <h2>Welcome to Nexa</h2>
        
        <form action="/login-email" method="POST">
            <input type="email" name="email" placeholder="Email Address" required>
            <input type="password" name="password" placeholder="Password" required>
            <button type="submit" class="btn btn-email">Login with Email</button>
        </form>

        <p>OR</p>
        <a href="/auth/discord" class="btn btn-discord">Login with Discord</a>
    </div>
</body>
</html>
